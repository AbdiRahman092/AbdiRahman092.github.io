# Gym Designer

A self-contained, single-file interactive gym floor-plan / equipment layout editor.
Open `index.html` in any browser (works great on mobile). Your layout autosaves to the
browser's local storage. Export/Import JSON to move a layout between devices.
